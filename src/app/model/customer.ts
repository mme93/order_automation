export interface Customer {
  id: string;
  firstName: string;
  lastName: string;
  email: string;
  city: string;
  street: string;
  postalCode: string;
  callNumber: string;
  information: string;
  company: string;
}
